export interface Image {
    publicURL: string;
}

export interface FetchFooterImagesQuery {
    mbancLogo: Image;
    hud: Image;
    nmls: Image;
    equal: Image;
    usda: Image;
    cotua: Image;
}
